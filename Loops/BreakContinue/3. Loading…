#include<stdio.h>

int main(void){

    int n, i;

    printf("Enter the starting point: ");
    scanf("%d", &n);

    printf("Enter the ending point: ");
    scanf("%d", &i);

    for(n; n<=i; n++){
        if(n%4==0){
            continue;
        }
        printf("Loading...%d%\n", n);
    }
}
