#include<stdio.h>

int main(void){
    
    int n;
    int i=0;
    
    printf("Enter n: ");
    scanf("%d", &n);
    
    while(n>i)
    {
        if(n%2==1)
        printf("%d\n", n);
        n--;
    }
}
